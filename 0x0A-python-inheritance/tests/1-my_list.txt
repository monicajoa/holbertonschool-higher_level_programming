#1- my_list.txt

====================================
    Tests using print sorted method
====================================

Prints the new list, but sorted (ascending)

    Import `MyList` from the file `1-my_list.py`:
>>> MyList = __import__('1-my_list').MyList

====================================

Test_1: Check printed list
    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list.append(4)
    >>> my_list.append(2)
    >>> my_list.append(3)
    >>> my_list.append(5)
    >>> my_list.print_sorted()
    [1, 2, 3, 4, 5]

 Test_2: Empty list
    >>> empty_list = MyList()
    >>> empty_list.print_sorted()
    []

Test_3: Is instance
    >>> isinstance(my_list, MyList)
    True

Test_4: Is Subclass
    >>> issubclass(type(my_list), MyList)
    True

Test_5: Mixed integers
    >>> my_list.append(-1)
    >>> my_list.append(-4)
    >>> my_list.append(-2)
    >>> my_list.append(-3)
    >>> my_list.append(-5)
    >>> my_list.print_sorted()
    [-5, -4, -3, -2, -1, 1, 2, 3, 4, 5]

Test_6: Negative integers
    >>> neg_list = MyList()
    >>> neg_list.append(-3)
    >>> neg_list.append(-11)
    >>> neg_list.append(-23)
    >>> neg_list.append(-69)
    >>> neg_list.append(-20)
    >>> neg_list.print_sorted()
    [-69, -23, -20, -11, -3]

Test_7: One element list
    >>> single_list = MyList()
    >>> single_list.append(30)
    >>> single_list.print_sorted()
    [30]